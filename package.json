{
  "name": "smartmark",
  "version": "1.0.0",
  "description": "SmartMark - AI-powered bookmark manager",
  "private": true,
  "scripts": {
    "install-bert": "npm install @xenova/transformers",
    "bundle": "webpack --config webpack.config.js",
    "build": "npm run install-bert && npm run bundle"
  },
  "devDependencies": {
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}
