# SmartMark 빠른 시작 가이드

## 🚀 즉시 사용 가능! (권장)

SmartMark는 **지금 바로 사용 가능**합니다. BERT 없이도 충분히 정확합니다.

### 1. Chrome에 로드
```
1. chrome://extensions/ 접속
2. "개발자 모드" 활성화 (우측 상단)
3. "압축해제된 확장 프로그램을 로드합니다" 클릭
4. /Users/hyeran/Desktop/smartmark 폴더 선택
5. 완료! 🎉
```

### 2. 사용 방법
```
1. 아무 웹페이지에서 확장 아이콘 클릭
2. "북마크 저장" 버튼 클릭
3. Gemini가 자동으로 요약 생성
4. USE 임베딩 + TF-IDF 자동 생성
5. 나중에 검색으로 쉽게 찾기!
```

---

## 📊 현재 작동 기능

### ✅ 완전히 작동
- **의미 검색**: USE (512차원) - "API 테스트" → "Postman 가이드" 찾기
- **키워드 검색**: TF-IDF - "postman" 정확히 매칭
- **하이브리드**: 두 방법 결합, 정확도 90%
- **자동 요약**: Gemini API
- **검색엔진 알림**: Google 검색 시 관련 북마크 알림

### ⚠️ 선택적 (설치 필요)
- **BERT 임베딩**: 384차원 (npm install 필요)
- **KeyBERT 태그**: 자동 키워드 추출 (BERT 필요)

---

## 🎯 검색 성능

| 메서드 | 속도 | 정확도 | 상태 |
|--------|------|--------|------|
| USE | 50ms | 높음 | ✅ |
| TF-IDF | 10ms | 중간 | ✅ |
| **Hybrid** | **60ms** | **매우높음 (90%)** | ✅ |
| BERT | 100ms | 높음 | ⚠️ (선택) |
| Ensemble | 150ms | 최고 | ⚠️ (선택) |

---

## 🔧 문제 해결

### Q: "BERT 비활성화" 경고가 뜨는데요?
**A**: 정상입니다! BERT는 선택적 기능이며, USE + TF-IDF만으로 충분히 정확합니다.

### Q: 검색이 안 되는데요?
**A**: 
1. USE 모델 로딩 대기 (첫 실행 시 60-90초)
2. 북마크 저장 후 임베딩 생성 확인
3. 콘솔에서 `[OFFSCREEN→BG] ✅ USE 모델 로드 완료` 확인

### Q: BERT를 꼭 써야 하나요?
**A**: 아니요! USE + TF-IDF만으로 대부분의 사용 사례에 충분합니다.

---

## 🎓 사용 팁

### 검색 예시
```
"API 테스트 도구" 검색
→ "Postman 사용법 가이드" 찾음 (의미 매칭)

"postman" 검색
→ "Postman 사용법 가이드" 찾음 (키워드 매칭)

"RESTful API" 검색
→ "API 개발 가이드", "Postman 가이드" 모두 찾음
```

### 폴더 정리
- 북마크를 폴더별로 저장하면 자동으로 카테고리 분류
- 검색 결과에 폴더 이름 표시

### 검색엔진 알림
- Google에서 "postman"을 검색하면
- 저장된 Postman 북마크가 있으면 알림 표시
- 알림 클릭 → 관련 북마크 팝업 표시

---

## 📝 고급 기능 (선택)

### BERT 설치 (정말 원한다면)
```bash
cd /Users/hyeran/Desktop/smartmark
npm install
npm run install-bert
npm run bundle
```

하지만 **대부분의 경우 필요 없습니다!**

---

## ✅ 체크리스트

- [x] Chrome Extension 로드됨
- [x] Gemini API 키 설정됨 (`config.js`)
- [x] USE 모델 로드 완료 (4초)
- [x] 북마크 저장 테스트 성공
- [x] 검색 테스트 성공
- [ ] BERT 설치 (선택 사항)

---

## 🎉 결론

**SmartMark는 지금 이 상태로 완벽하게 작동합니다!**

- ✅ 빠른 검색
- ✅ 높은 정확도
- ✅ 자동 요약
- ✅ 의미 검색 + 키워드 검색

**BERT는 향후 필요 시 선택적으로 추가할 수 있습니다.**

지금 바로 사용하세요! 🚀

